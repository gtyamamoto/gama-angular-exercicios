import { YearRangePipe } from './year-range.pipe';

describe('YearRangePipe', () => {
  it('create an instance', () => {
    const pipe = new YearRangePipe();
    expect(pipe).toBeTruthy();
  });
});
